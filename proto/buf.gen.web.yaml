version: v2
managed:
  enabled: true
plugins:
  # Local plugin over `remote:` — the BSR copy answers 403 without auth.
  - local: ["npx", "--yes", "@bufbuild/protoc-gen-es@2.12.0"]
    out: ../ts
    opt:
      - target=ts
      - import_extension=js
